/* ============================================================================
   Dashboard Configuration — Template
   Copy this file to config.js and replace the placeholder values with
   your real API keys. config.js is gitignored and will never be committed.
   ========================================================================== */

window.DASHBOARD_KEYS = {
  TRAPNZ_API_KEY: 'YOUR_TRAPNZ_API_KEY_HERE',
  MAPBOX_TOKEN:   'YOUR_MAPBOX_TOKEN_HERE',
};
